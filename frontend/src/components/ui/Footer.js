import React from 'react';

export default function Footer({ year }) {
    return (
        <footer className="footer mt-auto py-3 bg-light fixed-bottom">
            <div className="container">
                <span className="text-muted">Curso de React 2025 - CESDE</span>
            </div>
        </footer>
    )
}